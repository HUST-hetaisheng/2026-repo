绘制一张图表，标题为"rules of score combining & couple elimination"，用于表示在不同seasons中采用的不同评分情况和淘汰机制。
评分规则有一对："combine by rank"(在season1-2,28-34采用),"combine by percentage"(在season3-27采用), 采用同一色系的2个颜色。淘汰机制有一对："eliminate the last"(在season1-27采用),"eliminate from the bottom two"(在season28-34采用),采用另一色系的2个颜色
整个图片有4条轨道，每条轨道表示一种评分规则/淘汰机制，当该规则在某一season采用时，该轨道的线段为实线，否则为虚线且颜色为浅灰色。轨道最上方标记周数。